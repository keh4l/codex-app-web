# codex-web 配置文件。复制为 .env 后修改。.env 已被 .gitignore 忽略。
# scripts/start 会在启动 node 前 source 本文件（set -a），因此这里的每个变量
# 都会在启动时被导出到环境变量中。
#
# 所有值都要加引号（本文件由 bash source；未加引号且含空格的值会被当成命令执行）。

# 作为 app-server 后端的 codex CLI 二进制路径。
# 留空则使用 PATH 中的 codex。
# macOS 示例（官方桌面 app 自带、已登录）：
#   CODEX_CLI_PATH="/Applications/Codex.app/Contents/Resources/codex"
CODEX_CLI_PATH=""

# 监听地址 / 端口（默认 127.0.0.1 / 8214）。
# 设为 HOST="0.0.0.0" 可监听所有网卡（能被其他机器 / 通过网络访问 —— 在 Linux
# 服务器上很有用）。
# ⚠️ 安全警告：0.0.0.0 会把 codex-web 暴露到网络。任何能访问该端口的人，都能
# 以你的身份在这台机器上运行 codex（执行命令、读写文件、使用你的凭据）。
# 请仅在可信网络中这样做，或在前面套一层 wireguard / tailscale / 带认证的反向
# 代理。详见 README 的 “security” 一节。
# HOST="127.0.0.1"
# PORT="8214"

# 访问认证。设置 AUTH_PASSWORD 后，所有页面请求和后端 WebSocket 都要求登录：
# 未登录访问会跳转到自带的登录页（/__auth/login），登录成功后下发 30 天有效
# 的会话 cookie（服务重启不掉线；改密码会让所有会话失效）。留空则不启用认证。
# 用户名默认 codex，可用 AUTH_USERNAME 修改。脚本/curl 也可以直接带
# HTTP Basic 头（curl -u 用户名:密码）访问，无需走登录页。
# 对外暴露（HOST="0.0.0.0"）时强烈建议设置。注意：经纯 http 传输时凭据和
# cookie 是明文的，公网环境请配合 https 反代或 wireguard/tailscale 使用。
# AUTH_USERNAME="codex"
# AUTH_PASSWORD=""

# 代理 —— 若你的网络无法直连 ab.chatgpt.com，在此设置。
# 不设置的话，UI 启动时会卡在 splash 约 10 秒，等待 Statsig 请求超时。
# 这些变量必须通过 npm start 生效（scripts/start 会在 node 启动前导出它们）。
# 直接用 node src/server/main.js 则无效，因为 node 在 bootstrap 阶段就读取
# NODE_USE_ENV_PROXY，早于任何进程内的加载逻辑。
# NODE_USE_ENV_PROXY="1"
# HTTP_PROXY="http://127.0.0.1:10808"
# HTTPS_PROXY="http://127.0.0.1:10808"
# NO_PROXY="localhost,127.0.0.1,::1"

# Telegram Bot 桥接（可选）。设置 TELEGRAM_BOT_TOKEN 即启用：在 Telegram 里给 bot
# 发消息会驱动一个 codex 会话，codex 跑完把回复发回来。留空则该功能关闭。
# ⚠️ 安全警告：这是又一个主机级访问入口。审批策略固定 never、默认无沙箱
#    （danger-full-access）—— allowlist 内任何人发消息 = 以服务进程身份在本机任意
#    执行命令、读写文件、使用你的 codex 凭据。务必保密 bot token 并严格设 allowlist。
#    详见 README 的“安全”一节。
#
# 从 @BotFather 创建 bot 获取 token。
# TELEGRAM_BOT_TOKEN=""
#
# 允许使用的 Telegram 数字 user id（逗号分隔）。安全闸门，强烈建议设置。
# 用 @userinfobot 查自己的 user id。设为 "*" 允许所有人（危险，仅限私有测试）。
# 未设置时无人可用（bot 会回显发送者 id 以便加入白名单）。
# TELEGRAM_ALLOWED_USER_IDS=""
#
# codex 会话的工作目录。留空则用服务进程的 cwd（通常是本仓库根）。
# TELEGRAM_DEFAULT_CWD=""
#
# 沙箱模式：read-only | workspace-write | danger-full-access。默认
# danger-full-access（完全访问，复刻参考项目）。要收紧可设 workspace-write 或
# read-only。非法值会回退默认并告警。
# TELEGRAM_SANDBOX_MODE="danger-full-access"
#
# codex 记忆功能（features.memories）。默认开启，对齐参考项目；设为
# false / 0 / no / off 关闭。
# TELEGRAM_MEMORIES="true"
#
# 流式输出（typing 状态 + 消息实时编辑）。默认开启：发完消息后 bot 显示“正在
# 输入”，并随 codex 干活把一条消息滚动更新到最终答案。注意 codex 是 item 级推送
# （非逐字），所以是“分步更新”而非逐字打字机。设为 false / 0 / no / off 关闭，
# 回退为 turn 完成后一次性发送。
# TELEGRAM_STREAMING="true"
