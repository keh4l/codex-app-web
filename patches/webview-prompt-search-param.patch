--- a/webview/assets/app-main-BcDiyTD0.js
+++ b/webview/assets/app-main-BcDiyTD0.js
@@ -1210,6 +1210,7 @@
 import "./katex-BEvuNVH0.js";
 import "./pdf-BnC38vL8.js";
 import {
+  _ as useSearchParams,
   a as Dc,
   c as Oc,
   d as kc,
@@ -23234,6 +23235,8 @@
     f = lv(),
     m = Ac(),
     h = m.state,
+    [searchParams] = useSearchParams(),
+    searchParamsPrompt = searchParams.get(`prompt`),
     { data: g } = O(`account-info`, {
       queryConfig: { enabled: a === `chatgpt` },
     }),
@@ -23584,6 +23587,8 @@
                                   ? (e) => (0, Z.jsx)(iA, { ...e })
                                   : void 0,
                                 canStartRealtimeConversation: !0,
+                                defaultText: searchParamsPrompt ?? void 0,
+                                initialPrompt: searchParamsPrompt ?? void 0,
                                 onLocalConversationCreated: z,
                                 externalFooterVariant: `home`,
                                 hideRunLocationDropdownOverride:
@@ -23617,6 +23622,8 @@
                         browserConversationId: L,
                         canStartRealtimeConversation: !0,
                         composerLayoutMode: `auto-single-line`,
+                        defaultText: searchParamsPrompt ?? void 0,
+                        initialPrompt: searchParamsPrompt ?? void 0,
                         onLocalConversationCreated: z,
                         showWorkspaceDropdownInFooter: !1,
                         hideRunLocationDropdownOverride: C.kind !== `git`,
--- a/webview/assets/composer-D1QtVouy.js
+++ b/webview/assets/composer-D1QtVouy.js
@@ -22539,6 +22539,7 @@
   showPlanKeywordSuggestion: m,
   composerModeAvailability: h,
   placeholderText: v,
+  initialPrompt,
   composerLayoutMode: b,
   hotkeyWindowHomeFooterControls: C,
   defaultCwd: E,
@@ -22977,13 +22978,20 @@
       (Nn && $i(Nn),
         On && Wa(On),
         Ga != null && Ha(Ga),
-        En && En !== X.getText() && X.setPromptText(En),
+        En && En !== X.getText()
+          ? X.setPromptText(En)
+          : !En &&
+              initialPrompt != null &&
+              initialPrompt !== `` &&
+              X.getText() === ``
+            ? X.setPromptText(initialPrompt)
+            : null,
         kn != null && kn.length > 0 && dc(kn),
         Mn != null && Mn.length > 0 && Zi(Mn));
     });
   (0, Z.useEffect)(() => {
     qa();
-  }, [En, On, kn, Mn, Ga]);
+  }, [En, On, kn, Mn, Ga, initialPrompt]);
   let [Ja, Ya] = Ot(`composer_prefill`),
     Xa = (0, Z.useEffectEvent)(() => {
       let e = Ja?.commentAttachments;
@@ -25168,6 +25176,8 @@
       hideRunLocationDropdownOverride: C,
       composerModeAvailability: w,
       placeholderText: T,
+      defaultText,
+      initialPrompt,
       composerLayoutMode: D,
       hotkeyWindowHomeFooterControls: O,
       defaultCwd: k,
@@ -25195,7 +25205,7 @@
     ? (V = t[1])
     : ((V = Ir(ie.value)), (t[0] = ie.value), (t[1] = V));
   let se = V,
-    ce = ie.get(Kr),
+    ce = defaultText ?? ie.get(Kr),
     H = B?.type === `local` ? B.localConversationId : null,
     U = Gn(H),
     W = a(mt, H),
@@ -25446,6 +25456,7 @@
         showPlanKeywordSuggestion: te,
         composerModeAvailability: w,
         placeholderText: T,
+        initialPrompt,
         composerLayoutMode: re,
         hotkeyWindowHomeFooterControls: O,
         defaultCwd: k,
