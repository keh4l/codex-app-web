--- a/webview/assets/composer-controller-dL4CgOni.js
+++ b/webview/assets/composer-controller-dL4CgOni.js
@@ -20246,8 +20246,18 @@
     u = new EventTarget(),
     d = new rl(),
     f = o,
+    inputModeDisabled = !1,
+    getAttributes = () =>
+      inputModeDisabled
+        ? { spellcheck: `true` }
+        : { spellcheck: `true`, inputmode: `none` },
+    setPointerInputMode = (e) => {
+      inputModeDisabled !== e &&
+        ((inputModeDisabled = e),
+        p.isDestroyed || p.setProps({ attributes: getAttributes }));
+    },
     p = new nc(null, {
-      attributes: { spellcheck: `true` },
+      attributes: getAttributes,
       state: G.create({
         schema: nl,
         doc:
@@ -20335,6 +20345,17 @@
         let t = p.state.apply(e);
         (p.updateState(t), u.dispatchEvent(new Event(we)));
       },
+      handleDOMEvents: {
+        mousedown(e, t) {
+          return (setPointerInputMode(!0), !1);
+        },
+        touchstart(e, t) {
+          return (setPointerInputMode(!0), !1);
+        },
+        blur(e, t) {
+          return (setPointerInputMode(!1), !1);
+        },
+      },
       handlePaste(e, t) {
         if (t.defaultPrevented) return !0;
         let n = t.clipboardData,
