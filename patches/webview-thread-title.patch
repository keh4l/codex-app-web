--- a/webview/assets/local-conversation-thread-D5wniwTZ.js
+++ b/webview/assets/local-conversation-thread-D5wniwTZ.js
@@ -16074,6 +16074,7 @@
     C = G(Pt, e) ?? !1,
     w = G(Vt, e),
     T = G(p, e),
+    codexHostedWindowTitle = G(Zo, e),
     E = G(xr, e) === `projectless`,
     D = G(fe, e),
     O = G(yr, e),
@@ -16108,6 +16109,10 @@
   (0, sC.useEffect)(() => {
     ie?.id == null || ie.readAt != null || re(ie.id);
   }, [ie?.id, ie?.readAt, re]);
+  (0, sC.useEffect)(() => {
+    let t = codexHostedWindowTitle?.trim();
+    t && (document.title = `${t} | Codex`);
+  }, [codexHostedWindowTitle]);
   let se = Be(),
     { agentMode: ce, shouldSendPermissionOverrides: le } = bo({
       conversationId: e,
