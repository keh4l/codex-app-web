--- a/webview/assets/local-conversation-thread-BpXdMdk5.js
+++ b/webview/assets/local-conversation-thread-BpXdMdk5.js
@@ -309,6 +309,7 @@
   xi as $t,
   xr as en,
   xt as tn,
+  yt as codexHostedWindowTitleAtom,
   z as nn,
   zr as rn,
 } from "./app-server-manager-signals-DixsiAkQ.js";
@@ -41855,6 +41856,7 @@
     C = a(Ct, e) ?? `needs_resume`,
     w = a(jt, e),
     T = a(Ht, e),
+    codexHostedWindowTitle = a(codexHostedWindowTitleAtom, e),
     E = a(ct, e) === `projectless`,
     O = a(ge, e),
     k = a(be, e),
@@ -41879,6 +41881,10 @@
   (0, Z.useEffect)(() => {
     G?.id == null || G.readAt != null || W(G.id);
   }, [G?.id, G?.readAt, W]);
+  (0, Z.useEffect)(() => {
+    let t = codexHostedWindowTitle?.trim();
+    t && (document.title = `${t} | Codex`);
+  }, [codexHostedWindowTitle]);
   let te = on(),
     { agentMode: q } = gi({ conversationId: e, hostId: S }),
     ne = x ? fe(x) : null,
